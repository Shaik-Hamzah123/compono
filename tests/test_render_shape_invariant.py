"""Asserts every rendered element is a real, editable OOXML shape
(never a flattened image or embedded video). See COMPONO_PLAN.md section 3
("Non-negotiable invariant"). Populated starting Step 7.
"""
