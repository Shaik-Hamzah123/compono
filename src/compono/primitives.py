"""Typed builder classes (human path). Pure sugar over the pydantic schema in schema.py.

Serializes to the identical dict shape the agent path (raw JSON/dict) uses —
see COMPONO_PLAN.md section 9. Populated starting Step 1.
"""
